# 安卓构建指南（APK）

本项目游戏本体是纯静态文件（`index.html` + `style.css` + `js/`），零依赖、零构建。打 APK 的推荐方案是 **[Capacitor](https://capacitorjs.com/)**：它把静态页面装进一个原生 Android WebView 壳，产出标准 Android 工程，再用 Gradle 出 APK。这与现有「Electron 打包 Windows 桌面版」思路完全对应，游戏代码零改动。

> 备选方案：TWA（Trusted Web Activity，用 [Bubblewrap](https://github.com/GoogleChromeLabs/bubblewrap) 把已部署的 HTTPS 站点包成 APK）。它要求游戏先部署到公网，且 APK 依赖在线加载；离线可玩性差，不推荐本项目使用。Capacitor 版自带全部资源，断网可玩，体验与桌面版一致。

---

## 0. 前置条件（一次性环境准备）

| 工具 | 版本要求 | 说明 |
|---|---|---|
| JDK | **21** | **Capacitor 8 要求 Java 21**（`sudo apt install openjdk-21-jdk`），系统默认 JDK 可保持 17，只需在 `android/gradle.properties` 加 `org.gradle.java.home=/usr/lib/jvm/java-21-openjdk-amd64` |
| Android SDK | API 34+ | 需 `platform-tools`、`platforms;android-34`、`build-tools;34.0.0` |
| Node | ≥ 18 | 已满足（与现有要求一致） |

两种安装方式任选其一：

**方式 A：只装命令行工具（轻量，够用）**

```bash
# JDK 21（Ubuntu/Debian，Capacitor 8 必需）
sudo apt install openjdk-21-jdk

# Android 命令行工具
mkdir -p ~/android-sdk/cmdline-tools
curl -o cmdtools.zip https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
unzip cmdtools.zip -d ~/android-sdk/cmdline-tools
mv ~/android-sdk/cmdline-tools/cmdline-tools ~/android-sdk/cmdline-tools/latest

# 写入环境变量（~/.zshrc）cmd
export ANDROID_HOME=$HOME/android-sdk
export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools

# 安装 SDK 组件（需接受许可）
sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"
```

**方式 B：装 Android Studio（图形界面，顺带管模拟器）**

从 <https://developer.android.com/studio> 下载，用它装好 SDK 后同样设置 `ANDROID_HOME`（默认路径 `~/Android/Sdk`）。

> **WSL2 注意**（本机环境是 WSL2）：
> - Gradle 命令行构建在 WSL2 内完全可用，推荐在 WSL 里装方式 A 的 SDK。
> - WSL2 内跑模拟器很折腾；真机调试需要 Windows 侧用 [usbipd](https://learn.microsoft.com/windows/wsl/connect-usb) 把 USB 设备转发进来，或干脆在 Windows 侧装 Android Studio 跑 `npx cap open android`。
> - 最省事的组合：**WSL 里 Gradle 出 APK → APK 文件拷到手机/模拟器安装测试**。

---

## 1. 安装 Capacitor 依赖

```bash
npm install -D @capacitor/core @capacitor/cli @capacitor/android
```

仅为打包需要（开发依赖），游戏本体零依赖不变。

## 2. 准备 web 资源目录（`www/`）

Capacitor 需要 `webDir` 指向一个只含游戏静态文件的目录。仓库根目录混有 `node_modules`、`tests` 等，不能直接用，先做一个搬运脚本。

新建 `scripts/stage-web.sh`：

```bash
#!/bin/sh
# 把游戏静态文件搬运到 www/，供 Capacitor 打包 Android
set -e
cd "$(dirname "$0")/.." || exit 1
rm -rf www
mkdir -p www
cp index.html style.css www/
cp -r js www/
echo "www/ 已就绪（index.html / style.css / js/）"
```

```bash
chmod +x scripts/stage-web.sh
```

## 3. 初始化 Capacitor 与 Android 工程

在仓库根目录新建 `capacitor.config.json`：

```json
{
  "appId": "com.yhliu.junqi",
  "appName": "军旗翻翻棋",
  "webDir": "www",
  "android": {
    "allowMixedContent": false
  }
}
```

- `appId`：Android 包名，**一经发布不可更改**，建议用倒序域名格式。
- `appName`：手机桌面/应用列表里显示的名字。

生成 Android 原生工程：

```bash
npx cap add android
```

产出 `android/` 目录（标准 Gradle 工程，可提交进 git）。

## 4. 在 `package.json` 加脚本

```json
"scripts": {
  "stage:web": "scripts/stage-web.sh",
  "android:add": "npm run stage:web && npx cap add android",
  "android:sync": "npm run stage:web && npx cap sync android",
  "android:open": "npx cap open android",
  "android:run": "npm run stage:web && npx cap run android",
  "dist:apk": "npm run stage:web && npx cap sync android && cd android && ./gradlew assembleRelease"
}
```

对应关系：`dist:apk` ≈ 现有的 `dist:win`。每次改完游戏代码，跑 `npm run android:sync` 再构建即可（`cap sync` 会把 `www/` 拷进 `android/app/src/main/assets/public/`）。

## 5. 构建调试版 APK（先跑通）

```bash
npm run android:sync
cd android && ./gradlew assembleDebug
```

产物：`android/app/build/outputs/apk/debug/app-debug.apk`。

安装到已连接的设备/模拟器（WSL2 内没连设备时，直接把这个 APK 文件发到手机上点开安装也行）：

```bash
adb install app/build/outputs/apk/debug/app-debug.apk
```

## 6. 构建发布版 APK（正式分发）

Android 要求 release 包必须签名。生成一个自签名密钥（**只需一次，妥善保管，丢了就无法给同一应用发更新**）：

```bash
keytool -genkey -v -keystore ~/junqi-release.keystore -alias junqi \
  -keyalg RSA -keysize 2048 -validity 10000
```

在 `android/app/build.gradle` 的 `android { }` 块内追加签名配置（密码可后续改从环境变量/本地文件读，避免提交明文）：

```groovy
signingConfigs {
  release {
    storeFile file(System.getProperty("user.home") + "/junqi-release.keystore")
    storePassword "你的密码"
    keyAlias "junqi"
    keyPassword "你的密码"
  }
}
buildTypes {
  release {
    signingConfig signingConfigs.release
    minifyEnabled false
  }
}
```

构建：

```bash
npm run dist:apk
```

产物：`android/app/build/outputs/apk/release/app-release.apk` —— 这就是发给别人的最终 APK，拷贝/网盘分发均可，安装时系统会提示「允许未知来源」属正常现象（与 Windows 侧 SmartScreen 提示同理）。

> 只发 APK、不上架 Google Play 的话，自签名即可。若日后上架，需要改出 AAB（`bundleRelease`）并使用 Play App Signing。

## 7. 应用图标

仓库已有 `desktop/icon.png`（1024×1024 即可用）。最省事的方式：

```bash
npm install -D @capacitor/assets
npx capacitor-assets generate --android --iconBackgroundColor '#ffffff' --iconBackgroundColorDark '#111111'
```

它会从 `assets/icon.png`（或指定 `--iconPath desktop/icon.png`）自动生成各密度的前景/背景图标。生成后重新 `npm run dist:apk`。

也可在 Android Studio 里手动：`npx cap open android` → 右键 `res` → New → Image Asset。

## 8. 版本号维护

两处需要与 `package.json` 的 `version` 同步（发新版时手动改）：

- `android/app/build.gradle`：`versionCode`（整数，每次发版 +1）、`versionName`（如 `"1.0.3"`）。

## 9. 移动端适配检查清单

游戏代码基本无需改动，但装上真机后逐项确认：

- [ ] **布局**：`index.html` 已有 `viewport` meta，CSS 为响应式，竖屏下棋盘 + 记牌器侧栏的堆叠表现需肉眼确认；不适再补媒体查询。
- [ ] **触控**：交互全是 click，WebView 中原生映射为 touch，直接可用；hover 提示类效果在触屏上无效（若有需另补）。
- [ ] **Web Worker（AI 后台思考）**：Capacitor WebView 以 `https://localhost` 提供页面，`new Worker('js/ai-worker.js')` 正常创建，困难/大师档不冻屏；万一异常会自动降级为同步思考，行为一致（`js/main.js:30` 已有该降级）。
- [ ] **音效**：Web Audio 程序化合成，无资源文件，WebView 内可用；确认 App 退到后台再回来音效正常。
- [ ] **持久化**：音效/难度设置走 `localStorage`，WebView 内有效；卸载 App 即清空，属预期。
- [ ] **返回键**：Capacitor 默认物理返回键直接退出 App。如希望「再按一次退出」或返回时弹确认，需装 `@capacitor/app` 监听 `backButton` 事件，属可选增强。
- [ ] **横竖屏**：默认随系统旋转。想锁定竖屏，在 `android/app/src/main/AndroidManifest.xml` 的 `<activity>` 上加 `android:screenOrientation="portrait"`。

## 10. 常见问题

| 现象 | 处理 |
|---|---|
| `sdkmanager` 卡在许可确认 | `yes \| sdkmanager --licenses` |
| Gradle 报 `SDK location not found` | 确认 `ANDROID_HOME` 已导出；或在 `android/local.properties` 写 `sdk.dir=/home/你/android-sdk` |
| 首次构建下载 Gradle 超时 | `services.gradle.org` 国内直连常超时；把 `android/gradle/wrapper/gradle-wrapper.properties` 的 `distributionUrl` 换成腾讯镜像 `https://mirrors.cloud.tencent.com/gradle/gradle-8.14.3-all.zip`，并把 `networkTimeout` 调大（本项目已配置） |
| 编译报 `invalid source release: 21` | Capacitor 8 需要 JDK 21；装 `openjdk-21-jdk` 并在 `android/gradle.properties` 设 `org.gradle.java.home`（本项目已配置） |
| 装到手机闪退 | `adb logcat \| grep -i crash` 看日志，多为 WebView 版本或签名问题 |
| 改了游戏代码 APK 没更新 | 忘了 `npm run android:sync`——`www/` 不会自动刷新 |

---

## 速查：全流程一页版

```bash
# 一次性
npm install -D @capacitor/core @capacitor/cli @capacitor/android
scripts/stage-web.sh && npx cap add android        # 生成 android/ 工程
npx capacitor-assets generate --android            # 图标（可选）

# 日常迭代
npm run android:sync                               # 改完游戏代码后同步
cd android && ./gradlew assembleDebug              # 调试包

# 发布
cd android && ./gradlew assembleRelease            # 签名 release 包
# → android/app/build/outputs/apk/release/app-release.apk
```
