# 安卓发布说明

本文记录安卓版的**当前状态与日常维护流程**；从零搭建环境的完整教程见 [安卓构建指南.md](安卓构建指南.md)。

## 当前状态（2026-09-03）

- 打包方案：Capacitor 8（静态页面 → Android WebView 壳），游戏代码零改动
- 应用：`com.yhliu.junqi`「军旗翻翻棋」，versionCode 1 / versionName 1.0.3（与 package.json 同步）
- 已产出**签名 release APK** 并经真机试玩验证：`android/app/build/outputs/apk/release/app-release.apk`（约 3.5 MB）
- 图标与启动屏：复用 `desktop/icon.png`，底色 `#15314A`，全套 74 个资源已入库
- 真机验证项：布局 / 触控 / AI（Web Worker 后台思考）/ 音效 / 持久化 均正常

## 产物与分发

```
npm run dist:apk     # 一条命令：同步 web 资源 → 签名 → 出 release APK
```

产物：`android/app/build/outputs/apk/release/app-release.apk`，直接发文件即可（微信/网盘/U盘）。
接收方首次安装需允许「未知来源/安装未知应用」，属自签名 APK 的正常提示。

## 日常迭代流程

改完游戏代码（`index.html` / `style.css` / `js/`）后：

```bash
npm run android:sync && cd android && ./gradlew assembleDebug    # 快速验证
npm run dist:apk                                                 # 出正式包
```

注意：`www/` 与 APK 内资源**不会自动更新**，必须先 `android:sync`。

## 发版 checklist

1. `package.json` 的 `version` +1（如 1.0.3 → 1.0.4）
2. `android/app/build.gradle`：`versionCode` +1、`versionName` 同步改
3. （可选）`npm run dist:win` 同步出桌面版
4. `npm run dist:apk` → 分发新 APK

## ⚠️ 密钥备份（重要）

- keystore：`~/junqi-release.keystore`，密码见 `android/keystore.properties`（本地文件，已 gitignore）
- **两者务必一起备份到网盘/U盘**。丢失 = 无法再以同一身份发更新（只能换包名重新发布）
- 密码文件模板：`android/keystore.properties.example`（入库，供新环境参照）

## 相关文件地图

| 文件 | 作用 |
|---|---|
| `capacitor.config.json` | 包名 / 应用名 / webDir |
| `scripts/stage-web.sh` | 搬运游戏静态文件到 `www/`（`npm run stage:web`） |
| `scripts/make-icons.sh` | 重新生成全套图标/启动屏（改了 `desktop/icon.png` 后跑） |
| `scripts/write-keystore-config.sh` | 交互式重写 `keystore.properties` |
| `android/` | Gradle 原生工程（提交入库；`build/`、`local.properties`、`keystore.properties` 已忽略） |
| `安卓构建指南.md` | 环境搭建 + 签名 + 常见问题（含国内网络镜像配置） |

## 已固化的环境配置（换机器需重做，详见指南）

- Gradle 发行包走腾讯镜像（`android/gradle/wrapper/gradle-wrapper.properties`）
- 构建用 JDK 21（`android/gradle.properties` 的 `org.gradle.java.home`，系统默认 JDK 17 不受影响）
- SDK 路径在 `android/local.properties`（不入库）
- sharp 的 libvips 二进制走 npmmirror（`scripts/make-icons.sh` 内）
