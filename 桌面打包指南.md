# 桌面打包指南（Windows / Linux）

游戏本体是纯静态文件（`index.html` + `style.css` + `js/`），零依赖、零构建。桌面版用 **Electron** 打包：`desktop/main.js` 只负责开窗加载页面，游戏代码零改动。

## 一键打包

```bash
scripts/dist-desktop.sh              # 打 Windows x64（默认）
scripts/dist-desktop.sh linux        # 打 Linux x64
scripts/dist-desktop.sh win linux    # 两个都打
scripts/dist-desktop.sh --dev        # 只补装本机运行用的 electron 二进制（npm start 用）
```

产物在 `dist/` 下：

| 平台 | 产物 | 分发方式 |
|---|---|---|
| Windows | `dist/军旗翻翻棋-win32-x64/军旗翻翻棋.exe` | 整个文件夹打 zip 发给对方，解压双击即玩，无需安装 |
| Linux | `dist/军旗翻翻棋-linux-x64/军旗翻翻棋` | 整个文件夹打 tar/zip 发给对方，解压后运行可执行文件 |

- 首次运行 Windows 可能弹 SmartScreen「未知发布者」（未购买代码签名证书的正常现象），点「更多信息 → 仍要运行」。
- 首版使用 Electron 默认图标；如需自定义，把 `desktop/icon.ico` 备好，在 Windows 上构建时加 `--icon=desktop/icon.ico`（Linux 宿主换 Windows 图标需安装 wine）。

## 脚本做了什么（手动流程备查）

脚本把以下步骤自动化了，手动执行等价于：

```bash
# 0. 走国内镜像（直连 npmjs.org / github 下载 electron 经常超时）
export ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"

# 1. 安装依赖（--ignore-scripts 原因见下）
npm install --ignore-scripts --registry=https://registry.npmmirror.com

# 2. 打包（版本号取自 package.json）
node_modules/.bin/electron-packager . 军旗翻翻棋 \
  --platform=win32 --arch=x64 --out=dist --overwrite \
  --app-version=$(node -p "require(\"./package.json\").version") \
  --ignore="^/(dist|docs|tests|scripts|desktop/make-icon\.js|\.claude|\.git|plans|www|android)"

# 3.（可选）补装本机运行二进制，让 npm start 可用
(cd node_modules/electron && node install.js)
```

Linux 打包把 `--platform=win32` 换成 `--platform=linux` 即可，其余不变。

## 踩过的坑（2026-09-11 实录）

1. **直连官方源超时**：`registry.npmjs.org` 频繁 ECONNRESET/ETIMEDOUT，electron 二进制（GitHub Releases）也难以下载。→ 全程走 npmmirror 镜像（registry + `ELECTRON_MIRROR`）。
2. **`sharp` 编译失败导致整个 install 回滚**：`sharp` 是 `@capacitor/assets`（仅安卓流程用）的依赖，在本机 node-gyp 编译失败，npm 会回滚全部安装，`node_modules` 直接消失。→ 用 `npm install --ignore-scripts` 跳过所有安装脚本：sharp 不再编译，桌面打包完全不受影响（electron-packager 自己下载所需平台的 zip，不依赖被跳过的 postinstall）。
3. **副作用**：`--ignore-scripts` 也会跳过 electron 的 postinstall，所以本机 `npm start` 起不来。→ 需要本地跑的时候执行 `scripts/dist-desktop.sh --dev` 或手动 `cd node_modules/electron && node install.js` 补装 Linux 二进制（zip 已在 `~/.cache/electron` 缓存，秒装）。
4. **`npm install <pkg>` 也会装 package.json 里的全部依赖**，所以不能靠「只装 electron + packager」绕过 sharp 问题。
